# Metadata extraction summary

Source: `local:rustms@no-git` (sha256: `no-git`)


## Emitted claims (3)

- **rustms-cargo-edition** — rustms declares Rust edition  
  `Cargo.toml::package.edition` = `2021`
- **rustms-cargo-name** — rustms declares Cargo package name  
  `Cargo.toml::package.name` = `rustms`
- **rustms-cargo-version** — rustms declares Cargo package version  
  `Cargo.toml::package.version` = `0.1.0`
